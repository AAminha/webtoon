import React from "react";

const WeekWebtoon = ({ key, title, author, category, pubperiod, image }) => {
    const []
    return (
        <div>
            <img src = { image } />
            <h3>{title}</h3>
            <h5>{author}</h5>

        </div>
    )
}

export default WeekWebtoon;