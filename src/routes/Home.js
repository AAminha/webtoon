import axios from "axios";
import React, { useState } from "react";

const Home = () => {
    const [webtoons, setWebtoons] = useState([]);
    setWebtoons = null;

    /*getWebtoons = async () => {
        const apiUrl = "dummy/webtoon.json";

        const webtoon = await axios.get(apiUrl);
        console.log(webtoon);
    };*/
};

export default Home;